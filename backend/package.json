{
  "name": "keystroker-backend",
  "version": "1.0.0",
  "description": "",
  "main": "build/app.js",
  "dependencies": {
    "@types/express": "^4.16.0",
    "@types/node": "^10.11.6",
    "express": "^4.16.3"
  },
  "devDependencies": {
    "typescript": "^3.1.2"
  },
  "scripts": {
    "postinstall": "cd ../frontend/ && npm run build && cp -r build/ ../backend/build/dist",
    "start": "tsc && node build/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
